<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" class="my-4" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">    
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-red">Supprimer</button>
</form>
