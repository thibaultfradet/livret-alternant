{% extends 'base.html.twig' %}

{% block title %}SchoolYear{% endblock %}

{% block body %}
    <h1>SchoolYear</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school_year.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ school_year.label }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ school_year.startDate ? school_year.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ school_year.endDate ? school_year.endDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ school_year.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_school_year_index') }}">back to list</a>

    <a href="{{ path('app_school_year_edit', {'id': school_year.id}) }}">edit</a>

    {{ include('school_year/_delete_form.html.twig') }}
{% endblock %}
