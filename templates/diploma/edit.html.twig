{% extends 'base.html.twig' %}

{% block title %}Modifier un diplôme{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="fw-bold text-primary mb-5 display-5 text-center">Modifier un diplôme</h1>

    <div class="card p-4 mb-4">
        {{ include('diploma/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-3">
        <a class="btn btn-outline-primary" href="{{ path('app_diploma_index') }}">Revenir à la liste</a>
        {{ include('diploma/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}