{% extends 'base.html.twig' %}

{% block title %}Ajouter un diplôme{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">Ajouter un diplôme</h1>

    <div class="card p-4 mb-4">
        {{ include('diploma/_form.html.twig', {'button_label': 'Créer'}) }}
    </div>

    <a class="btn btn-outline-secondary" href="{{ path('app_diploma_index') }}">Revenir à la liste</a>
</div>
{% endblock %}