{% extends 'base.html.twig' %}

{% block title %}Créer une classe{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="fw-bold text-primary mb-5 display-5 text-center">Créer une classe</h1>

   {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('training_parameters_classroom') }}" class="btn btn-outline-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}